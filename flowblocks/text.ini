[block]
name = Text Block
button_label = Text

[fields.text]
label = Text
type = markdown

[fields.alignment]
label = Align
type = select
choices = left, center, right
choice_labels = Left, Center, Right
default = left

